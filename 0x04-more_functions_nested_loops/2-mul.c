#include "main.h"
#include <stdio.h>
/**
 * mul - multiplies two given numbers
 *@a: first number to multiply
 *@b: second number to multiply
 * Return: a times b.
 */

int mul(int a, int b)
{
	return (a * b);
}
