#include <stdio.h>
#include <stdlib.h>
#include "main.h"
/**
  *print_last_digit - program gets the last digit of input
  *@a: input
  *Return: returns last digit of a
  */
int print_last_digit(int a)
{
	return (a % 10);
}
