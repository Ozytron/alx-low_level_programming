#ifndef FUNCTION_LIKE_MICRO_H
#define FUNCTION_LIKE_MICRO_H

/**
 * Description: a function-like macro ABS(x) that
 *              computes the absolute value of a number x.
 */

#define ABS(x) ((x) < 0 ? (-x) : (x))

#endif /* FUNCTION_LIKE_MICRO_H */
